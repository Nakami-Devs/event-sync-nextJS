# PostgreSQL database
# Replace USER, PASSWORD and DB_NAME with your local psql credentials
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/DB_NAME"

# NextAuth — generates a random value using: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"
